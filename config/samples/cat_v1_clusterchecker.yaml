apiVersion: cat.kubecat.com/v1
kind: ClusterChecker
metadata:
  labels:
    app.kubernetes.io/name: kube-cat
    app.kubernetes.io/managed-by: kustomize
  name: clusterchecker-sample
spec:
  # TODO(user): Add fields here
